# Legacy Tk GUI

The default application entry is now the **Electron + React** desktop (`run.bat` �� `desktop/`).

This Tk stack (`app.py`, `analysis_controller.py`, etc.) is kept for migration and troubleshooting only. Prefer `run_legacy.bat` if you still need Tk.
